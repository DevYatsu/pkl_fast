ENV_VALUE="TEST_VALUE"
s="str"
greeting = "Hi,\u{1F60A} \t my friend! \u{1E0A} \n How are you? \n \(ENV_VALUE + s)"


x: Map<Int, String>(length <= 5) = Map(0, "0")

class Bird {
  name: String(length >= 3)   
  parent: String(this != name)  
}

emailRegex: Regex = Regex("test")
